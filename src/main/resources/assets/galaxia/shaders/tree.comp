#version 430

struct tree {
    uint[] leaves; // index of sub trees
    uint leafFlags; // which sub trees exist (last 8 bits only), first 24 bits are particle count

    uint[] index; // list of particle indexes, each particle on creation figures out its leaf and creates one if it doesnt exist
};

void main() {

}
